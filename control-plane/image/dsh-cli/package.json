{
  "name": "@zhming0/dsh-yawn-image-cli",
  "private": true,
  "description": "The dsh CLI tree the control-plane image runs, locked outside the workspace so the image never resolves it fresh.",
  "dependencies": {
    "@deepseek-ai/dsh": "0.1.7-rc.2"
  }
}
