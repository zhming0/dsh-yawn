// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: dsh/yawn/v1/terminal.proto

package yawnv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/zhming0/dsh-yawn/runner/gen/dsh/yawn/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// TerminalServiceName is the fully-qualified name of the TerminalService service.
	TerminalServiceName = "dsh.yawn.v1.TerminalService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// TerminalServiceTerminalProcedure is the fully-qualified name of the TerminalService's Terminal
	// RPC.
	TerminalServiceTerminalProcedure = "/dsh.yawn.v1.TerminalService/Terminal"
)

// TerminalServiceClient is a client for the dsh.yawn.v1.TerminalService service.
type TerminalServiceClient interface {
	// One interactive terminal session: PTY allocation, input, resize, and
	// output multiplexed over one stream. See the message docs for the ordering
	// and shutdown contract.
	Terminal(context.Context) *connect.BidiStreamForClient[v1.TerminalRequest, v1.TerminalResponse]
}

// NewTerminalServiceClient constructs a client for the dsh.yawn.v1.TerminalService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewTerminalServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) TerminalServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	terminalServiceMethods := v1.File_dsh_yawn_v1_terminal_proto.Services().ByName("TerminalService").Methods()
	return &terminalServiceClient{
		terminal: connect.NewClient[v1.TerminalRequest, v1.TerminalResponse](
			httpClient,
			baseURL+TerminalServiceTerminalProcedure,
			connect.WithSchema(terminalServiceMethods.ByName("Terminal")),
			connect.WithClientOptions(opts...),
		),
	}
}

// terminalServiceClient implements TerminalServiceClient.
type terminalServiceClient struct {
	terminal *connect.Client[v1.TerminalRequest, v1.TerminalResponse]
}

// Terminal calls dsh.yawn.v1.TerminalService.Terminal.
func (c *terminalServiceClient) Terminal(ctx context.Context) *connect.BidiStreamForClient[v1.TerminalRequest, v1.TerminalResponse] {
	return c.terminal.CallBidiStream(ctx)
}

// TerminalServiceHandler is an implementation of the dsh.yawn.v1.TerminalService service.
type TerminalServiceHandler interface {
	// One interactive terminal session: PTY allocation, input, resize, and
	// output multiplexed over one stream. See the message docs for the ordering
	// and shutdown contract.
	Terminal(context.Context, *connect.BidiStream[v1.TerminalRequest, v1.TerminalResponse]) error
}

// NewTerminalServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewTerminalServiceHandler(svc TerminalServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	terminalServiceMethods := v1.File_dsh_yawn_v1_terminal_proto.Services().ByName("TerminalService").Methods()
	terminalServiceTerminalHandler := connect.NewBidiStreamHandler(
		TerminalServiceTerminalProcedure,
		svc.Terminal,
		connect.WithSchema(terminalServiceMethods.ByName("Terminal")),
		connect.WithHandlerOptions(opts...),
	)
	return "/dsh.yawn.v1.TerminalService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case TerminalServiceTerminalProcedure:
			terminalServiceTerminalHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedTerminalServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedTerminalServiceHandler struct{}

func (UnimplementedTerminalServiceHandler) Terminal(context.Context, *connect.BidiStream[v1.TerminalRequest, v1.TerminalResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("dsh.yawn.v1.TerminalService.Terminal is not implemented"))
}
